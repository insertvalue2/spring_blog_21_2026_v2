# 결제 내역 조회 기능 작업 내용

## 📋 개요

사용자가 포인트 충전을 위해 결제한 내역을 조회하는 기능입니다.
로그인한 사용자만 자신의 결제 내역을 확인할 수 있습니다.

---

## 🎯 주요 기능

- 로그인한 사용자의 결제 내역 목록 조회
- 결제 정보 표시 (주문번호, 결제번호, 결제일시, 결제 금액, 상태)
- 결제 상태 표시 (결제완료, 환불완료)
- 결제 내역이 없을 경우 안내 메시지 표시

---

## 📁 관련 파일 구조

```
src/main/java/org/example/demo_ssr_v1_1/
├── user/
│   └── UserController.java          # 결제 내역 조회 엔드포인트
├── payment/
│   ├── Payment.java                 # 결제 내역 엔티티
│   ├── PaymentRepository.java       # 결제 내역 데이터 조회
│   ├── PaymentService.java          # 결제 내역 비즈니스 로직
│   └── PaymentResponse.java         # 결제 내역 응답 DTO
└── _core/
    └── utils/
        └── MyDateUtil.java          # 날짜 포맷팅 유틸리티

src/main/resources/templates/
└── user/
    ├── detail.mustache              # 회원정보 페이지 (결제 내역 버튼 추가)
    └── payment-list.mustache        # 결제 내역 화면 템플릿
```

---

## 🔄 동작 흐름

```
1. 사용자 요청
   GET /user/payment/list
   ↓
2. UserController.paymentList()
   - 세션에서 로그인 사용자 정보 추출
   - PaymentService.결제내역조회() 호출
   ↓
3. PaymentService.결제내역조회()
   - PaymentRepository.findAllByUserId() 호출
   - Payment 엔티티 리스트를 DTO로 변환
   ↓
4. PaymentRepository.findAllByUserId()
   - 사용자 ID로 결제 내역 조회
   - 생성일 기준 내림차순 정렬 (최신순)
   ↓
5. PaymentResponse.ListDTO 변환
   - Payment 엔티티 → ListDTO 변환
   - 날짜 포맷팅 처리
   - 상태 표시명 변환
   ↓
6. View 렌더링
   - payment-list.mustache 템플릿에 데이터 전달
   - 결제 내역 목록 화면 표시
```

---

## 💻 코드 상세 설명

### 1. Controller Layer

**파일**: `UserController.java`

```java
@GetMapping("/user/payment/list")
public String paymentList(Model model, HttpSession session) {
    User sessionUser = (User) session.getAttribute("sessionUser");
    List<PaymentResponse.ListDTO> paymentList = paymentService.결제내역조회(sessionUser.getId());
    model.addAttribute("paymentList", paymentList);
    return "user/payment-list";
}
```

**역할**:
- HTTP GET 요청 처리
- 세션에서 로그인 사용자 정보 추출
- Service 레이어에 비즈니스 로직 위임
- View에 데이터 전달

**인증/인가**:
- `LoginInterceptor`가 `/user/**` 경로를 인터셉트하여 로그인 여부 확인
- 로그인하지 않은 사용자는 `/login` 페이지로 리다이렉트

---

### 2. Service Layer

**파일**: `PaymentService.java`

```java
@Transactional(readOnly = true)
public List<PaymentResponse.ListDTO> 결제내역조회(Long userId) {
    List<Payment> paymentList = paymentRepository.findAllByUserId(userId);
    return paymentList.stream()
            .map(PaymentResponse.ListDTO::new)
            .collect(java.util.stream.Collectors.toList());
}
```

**역할**:
- 비즈니스 로직 처리
- Repository에서 결제 내역 조회
- 엔티티를 DTO로 변환하여 반환

**트랜잭션**:
- `@Transactional(readOnly = true)`: 읽기 전용 트랜잭션
- 조회만 수행하므로 읽기 전용으로 설정하여 성능 최적화

**OSIV False 환경 대응**:
- 트랜잭션 내에서 필요한 데이터를 모두 조회
- 엔티티를 DTO로 변환하여 반환 (LAZY 로딩 문제 방지)

---

### 3. Repository Layer

**파일**: `PaymentRepository.java`

```java
@Query("""
    SELECT p FROM Payment p
    WHERE p.user.id = :userId
    ORDER BY p.createdAt DESC
    """)
List<Payment> findAllByUserId(@Param("userId") Long userId);
```

**역할**:
- 사용자 ID로 결제 내역 조회
- 생성일 기준 내림차순 정렬 (최신순)

**생성되는 SQL**:
```sql
SELECT p.* 
FROM payment_tb p
WHERE p.user_id = ?
ORDER BY p.created_at DESC
```

---

### 4. Response DTO

**파일**: `PaymentResponse.java`

```java
@Data
public static class ListDTO {
    private Long id;
    private String impUid;          // 포트원 결제 번호
    private String merchantUid;    // 주문 번호
    private Integer amount;         // 결제 금액
    private String status;          // 결제 상태 (paid, cancelled)
    private String statusDisplay;   // 상태 표시명
    private String paidAt;         // 결제일시

    public ListDTO(Payment payment) {
        this.id = payment.getId();
        this.impUid = payment.getImpUid();
        this.merchantUid = payment.getMerchantUid();
        this.amount = payment.getAmount();
        this.status = payment.getStatus();

        // 상태 표시명 변환
        if ("paid".equals(payment.getStatus())) {
            this.statusDisplay = "결제완료";
        } else if ("cancelled".equals(payment.getStatus())) {
            this.statusDisplay = "환불완료";
        } else {
            this.statusDisplay = payment.getStatus();
        }

        // 날짜 포맷팅
        if (payment.getCreatedAt() != null) {
            this.paidAt = MyDateUtil.timestampFormat(payment.getCreatedAt());
        }
    }
}
```

**역할**:
- Payment 엔티티를 화면에 표시할 수 있는 DTO로 변환
- 날짜 포맷팅 처리
- 상태 표시명 변환 (paid → 결제완료, cancelled → 환불완료)
- 필요한 정보만 추출하여 전달

**날짜 포맷팅**:
- `MyDateUtil.timestampFormat()`: `yyyy-MM-dd HH:mm` 형식으로 변환
- 예: `2024-01-15 14:30`

**상태 표시명 변환**:
- `paid` → `결제완료`
- `cancelled` → `환불완료`

---

### 5. Entity

**파일**: `Payment.java`

```java
@Entity
@Table(name = "payment_tb")
public class Payment {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(unique = true, nullable = false)
    private String impUid;          // 포트원 결제 고유 번호

    @Column(unique = true, nullable = false)
    private String merchantUid;    // 가맹점 주문 번호

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "user_id", nullable = false)
    private User user;              // 결제한 사용자

    @Column(nullable = false)
    private Integer amount;         // 결제 금액 (포인트)

    @Column(nullable = false)
    private String status;          // 결제 상태 (paid: 결제 완료, cancelled: 취소됨)

    @CreationTimestamp
    private Timestamp createdAt;   // 결제일시
}
```

**역할**:
- 결제 내역 정보를 저장하는 엔티티
- 포트원(PortOne) 결제 정보를 저장

**연관관계**:
- `Payment -> User` (ManyToOne): 결제한 사용자
- LAZY 로딩 사용 (필요할 때만 조회)

**결제 상태**:
- `paid`: 결제 완료
- `cancelled`: 환불 완료

---

### 6. View Template

**파일**: `payment-list.mustache`

```mustache
{{#paymentList}}
<div class="card mb-4">
    <div class="card-body">
        <h5 class="card-title mb-2">결제 #{{id}}</h5>
        <p class="text-muted mb-2">
            <i class="fas fa-receipt"></i> 주문번호: {{merchantUid}}
        </p>
        <p class="text-muted mb-2">
            <i class="fas fa-key"></i> 결제번호: {{impUid}}
        </p>
        <p class="text-muted mb-2">
            <i class="fas fa-calendar"></i> 결제일: {{paidAt}}
        </p>
        <p class="text-muted mb-0">
            <i class="fas fa-info-circle"></i> 상태: 
            <span class="badge bg-success">{{statusDisplay}}</span>
        </p>
        <div class="text-end">
            <span class="badge bg-primary fs-6">+{{amount}} P</span>
        </div>
    </div>
</div>
{{/paymentList}}

{{^paymentList}}
<div class="text-center py-5">
    <i class="fas fa-credit-card fa-3x text-muted mb-3"></i>
    <h5 class="text-muted">결제 내역이 없습니다</h5>
    <p class="text-muted">포인트를 충전해보세요!</p>
    <a href="/user/point/charge" class="btn btn-primary">포인트 충전하기</a>
</div>
{{/paymentList}}
```

**역할**:
- 결제 내역 목록을 화면에 표시
- 결제 내역이 없을 경우 안내 메시지 표시
- 각 결제 내역을 카드 형태로 표시

**표시 정보**:
- 결제 ID
- 주문번호 (merchantUid)
- 결제번호 (impUid)
- 결제일시 (포맷된 날짜)
- 결제 상태 (결제완료/환불완료)
- 결제 금액 (+포인트)

---

### 7. 회원정보 페이지 버튼 추가

**파일**: `detail.mustache`

```mustache
<div class="d-grid gap-2">
    <a href="/user/point/charge" class="btn btn-success">포인트 충전</a>
    <a href="/user/payment/list" class="btn btn-warning">결제 내역</a>
    <a href="/user/purchase/list" class="btn btn-info">구매 내역</a>
    ...
</div>
```

**역할**:
- 회원정보 페이지에서 결제 내역 페이지로 이동할 수 있는 버튼 제공
- "포인트 충전" 버튼 아래에 "결제 내역" 버튼 추가

---

## 🔑 핵심 기술 포인트

### 1. Stream을 활용한 DTO 변환

```java
return paymentList.stream()
        .map(PaymentResponse.ListDTO::new)  // 메서드 참조 사용
        .collect(java.util.stream.Collectors.toList());
```

**장점**:
- 간결하고 읽기 쉬운 코드
- 함수형 프로그래밍 스타일
- 불변 컬렉션 생성

### 2. OSIV False 환경 대응

**OSIV (Open Session In View) False 설정**:
```yaml
spring:
  jpa:
    open-in-view: false
```

**영향**:
- 트랜잭션이 종료되면 영속성 컨텍스트가 닫힘
- LAZY 로딩이 트랜잭션 밖에서 실행되면 `LazyInitializationException` 발생

**대응 방법**:
1. 트랜잭션 내에서 필요한 데이터를 모두 조회
2. 엔티티를 DTO로 변환하여 반환 (LAZY 로딩 불필요)

### 3. 상태 표시명 변환

```java
if ("paid".equals(payment.getStatus())) {
    this.statusDisplay = "결제완료";
} else if ("cancelled".equals(payment.getStatus())) {
    this.statusDisplay = "환불완료";
}
```

**목적**:
- DB에 저장된 영문 상태값을 한글로 변환하여 사용자에게 표시
- 사용자 친화적인 UI 제공

---

## 📊 데이터베이스 스키마

### payment_tb 테이블

| 컬럼명 | 타입 | 설명 |
|--------|------|------|
| id | BIGINT | 기본키 (자동 증가) |
| imp_uid | VARCHAR | 포트원 결제 고유 번호 (유니크) |
| merchant_uid | VARCHAR | 가맹점 주문 번호 (유니크) |
| user_id | BIGINT | 결제한 사용자 ID (FK) |
| amount | INT | 결제 금액 (포인트) |
| status | VARCHAR | 결제 상태 (paid, cancelled) |
| created_at | TIMESTAMP | 결제일시 (자동 생성) |

**유니크 제약조건**:
- `imp_uid`: 포트원 결제 고유 번호는 중복될 수 없음
- `merchant_uid`: 가맹점 주문 번호는 중복될 수 없음

---

## ✅ 테스트 시나리오

### 정상 케이스

1. **로그인한 사용자가 결제 내역 조회**
   - 요청: `GET /user/payment/list`
   - 결과: 결제한 내역 목록이 최신순으로 표시됨

2. **결제 내역이 있는 경우**
   - 주문번호, 결제번호, 결제일시, 결제 금액, 상태가 표시됨
   - 상태는 "결제완료" 또는 "환불완료"로 표시됨

3. **결제 내역이 없는 경우**
   - "결제 내역이 없습니다" 메시지 표시
   - "포인트 충전하기" 버튼 제공

### 예외 케이스

1. **로그인하지 않은 사용자**
   - `LoginInterceptor`가 요청을 차단
   - `/login` 페이지로 리다이렉트

2. **세션 만료**
   - `Exception401` 발생
   - 로그인 페이지로 리다이렉트

---

## 🎓 학습 포인트

### 1. 계층 구조 (3-Tier Architecture)

```
Controller (표현 계층)
    ↓
Service (비즈니스 계층)
    ↓
Repository (데이터 접근 계층)
    ↓
Database
```

각 계층의 역할을 명확히 분리하여 유지보수성 향상

### 2. DTO 패턴

- 엔티티를 직접 반환하지 않고 DTO로 변환
- 필요한 정보만 노출하여 보안성 향상
- 화면에 맞는 데이터 구조 제공

### 3. 트랜잭션 관리

- 읽기 전용 트랜잭션으로 성능 최적화
- 트랜잭션 범위를 적절히 설정하여 데이터 일관성 보장

### 4. 날짜 포맷팅

- DB에 저장된 Timestamp를 사용자 친화적인 형식으로 변환
- `MyDateUtil` 유틸리티 클래스 활용

---

## 📝 참고 사항

- 결제 내역은 생성일 기준 내림차순으로 정렬 (최신순)
- 결제 금액은 양수로 표시 (`+5000 P`)
- 결제 상태는 한글로 표시 (결제완료, 환불완료)
- 결제 내역이 없을 경우 포인트 충전 페이지로 이동할 수 있는 버튼 제공
- 회원정보 페이지에서 "결제 내역" 버튼을 통해 접근 가능

---

## 🔗 관련 기능

- **포인트 충전**: `PaymentService.결제검증및충전()` - 포인트 충전 시 결제 내역 저장
- **구매 내역**: `PurchaseService.구매내역조회()` - 유료 게시글 구매 내역 조회

---

**작성일**: 2024년  
**작성자**: 개발팀  
**버전**: 1.0
